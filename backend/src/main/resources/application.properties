# ──────────────────────────────────────────────
# SAGRES Monitoria — application.properties
# Configurações base (commitável).
# Secrets ficam em application-dev.properties (gitignored).
# ──────────────────────────────────────────────

spring.application.name=sagres-monitoria

# Ativa o perfil dev por padrão em ambiente local
# Em prod, sobrescrever via variável de ambiente:
# SPRING_PROFILES_ACTIVE=prod
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
