package com.sagres.modulo_usuario.domain.entity;

/**
 * Papel do usuário na plataforma SAGRES.
 * Um usuário pode ser ALUNO (busca monitorias) ou MONITOR (oferece monitorias).
 */
public enum RoleUsuario {
    ALUNO,
    MONITOR
}
